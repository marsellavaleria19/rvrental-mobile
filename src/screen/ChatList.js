import React from 'react';
import {View, Text} from 'react-native';

const ChatList = () => {
   return (
      <View>
         <Text>Chat List</Text>
      </View>
   );
};

export default ChatList;
